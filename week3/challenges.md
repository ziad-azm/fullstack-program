# Main Challenges — Week 3 Frontend Self-Study

- **New to Angular's structure** — getting used to components, services, and dependency injection coming from a backend (Node.js) background.
- **Reactive forms** — setting up `FormGroup`/`FormControl` and showing validation messages per field.
- **API integration** — wiring `HttpClient`, handling Observables, and managing loading and error states.
- **CORS** — the FastAPI backend had to allow requests from the Angular dev server (`http://localhost:4200`).
- **(add your own as they come up)** **********************\_\_**********************
